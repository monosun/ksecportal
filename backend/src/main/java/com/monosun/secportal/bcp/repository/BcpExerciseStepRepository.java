package com.monosun.secportal.bcp.repository;

import com.monosun.secportal.bcp.entity.BcpExerciseStep;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BcpExerciseStepRepository extends JpaRepository<BcpExerciseStep, Long> {
}
